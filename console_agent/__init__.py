"""Local serial console agent.

The agent is intentionally independent from Django. Django can publish PC and
device settings, while browsers connect directly to this service for console
traffic.
"""

